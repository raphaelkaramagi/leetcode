# leetcode

Auto-synced LeetCode submissions. Each accepted solve is stored as `####-problem-slug/` with a LeetCode problem statement (`README.md`) and code (`solution.*`).

Solutions are backdated to when each problem was solved. New solves sync daily via GitHub Actions ([`sync_leetcode.yml`](.github/workflows/sync_leetcode.yml)).
